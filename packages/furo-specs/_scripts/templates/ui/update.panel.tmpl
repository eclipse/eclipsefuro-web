// Code generated by @furo/specs. DO NOT EDIT.
// source: {{.source}}
import {html, css} from 'lit-element';
import {FBP} from "@furo/fbp";
import {BasePanel} from "@furo/route/lib/BasePanel";

import '@furo/data';
import '@furo/layout';

{{range $import := .imports}}
import "{{$import}}";{{end}}

/**
 * {{.description}}
 *
 * @customElement
 * @appliesMixin FBP
 */
export class {{.class_name}} extends FBP(BasePanel) {

    static get styles() {
        // language=CSS
        return [
            css`
                :host {
                    display: block;
                    height: 100%;
                    background-color: var(--surface-light);
                    color: var(--on-surface);
                    padding-top: var(--spacing);
                }

                :host([hidden]) {
                    display: none;
                }

                furo-card {
                    margin: 0 var(--spacing);
                    margin-bottom: var(--spacing);
                }
            `
        ];
    }

    /**
     * @private
     * @returns {TemplateResult}
     */
    render() {
        // language=HTML
        return html`
          {{"<" | noescape}}furo-vertical-flex>
            {{"<" | noescape}}furo-card>
              {{"<" | noescape}}!-- FORM -->
              {{"<" | noescape}}{{.form.name}}{{range $attr := .form.attrs}} {{$attr}}{{end}} ƒ-bind-data="--data(*.data)">{{"<" | noescape}}/{{.form.name}}>
            </furo-card>
            {{"<" | noescape}}!-- ACTION -->
            {{"<" | noescape}}{{.action.name}}{{range $attr := .action.attrs}} {{$attr}}{{end}} @-update="--updateRequested">{{"<" | noescape}}/{{.action.name}}>
            {{"<" | noescape}}!-- SUBS -->

          </furo-vertical-flex>

          {{"<" | noescape}}!-- non visuell components -->
          {{"<" | noescape}}furo-entity-agent service="{{.service_name}}"
                             ƒ-hts-in="--navNode(*.value.link)"
                             ƒ-save="--updateRequested"
                             ƒ-bind-request-data="--data(*.data)"
                             load-on-hts-in
                             @-response="--response"></furo-entity-agent>

          {{"<" | noescape}}furo-data-object type="{{.response_type}}"
                            @-object-ready="--data"
                            ƒ-inject-raw="--response"></furo-data-object>

        `;
    }


}

window.customElements.define('{{.component_name}}', {{.class_name}});
